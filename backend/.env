# =========================================================
# .env - Environment variables for local development
# =========================================================

# --- Application Configuration ---

# The address and port the Axum server will listen on
LISTEN_HOST=127.0.0.1
LISTEN_PORT=8080

# The root directory where your S3 buckets will be stored on the local filesystem.
# This path is relative to the project root.
STORAGE_ROOT=./data/storage

# --- Security Configuration ---

# ⚠️ CRITICAL: The secret key used to sign and verify JSON Web Tokens (JWT).
# This MUST be a strong, random string (at least 32 characters is recommended for HS256).
# DO NOT commit this file with a weak or default secret to public repositories.
JWT_SECRET="JkF4V5zQp8gYt3LbRm2sH6uI1xW7cE0oD9fG4vZ0kC1rT9aN2bX3jP5yM8qO"

# The duration (in hours) before a generated JWT expires
JWT_EXPIRATION_HOURS=24

# .env
MOJO_ROOT_USER=admin
MOJO_ROOT_PASSWORD=My$ecureP@ssword!
